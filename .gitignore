# Secrets / credentials
credentials.json
token.json
.env
# Streamlit: secrets stay local, but config.toml (upload limits) is shipped
.streamlit/*
!.streamlit/config.toml

# PII — never commit
profile/
resume/master_resume.md
resume/tailored/

# Run artifacts
data/
logs/

# Python
__pycache__/
*.pyc
.venv/
venv/

# pip install artefacts (e.g. `pip install streamlit>=1.35` creates `=1.35`)
=*
